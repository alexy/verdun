# Verdun App State

## Goal

Build a newsletter generator for strongly typed and functional AI/data news. The deployed Vercel app should show candidate news items, allow upvote/downvote editorial triage, accept text requests for what to collect more of, and later publish polished weekly updates through the Ghost API to `collected.ga`.

## Current Slice

- Vue/Vite app with a newsroom triage interface.
- Vercel API routes:
  - `GET /api/newsletter/items`
  - `POST /api/newsletter/vote`
  - `POST /api/newsletter/focus`
- External Postgres schema in `db/migrations/0001_newsletter.sql`.
- Rust crawler/loader scaffold under `crawler/`.
- Static fallback data in `public/data/newsletter-snapshot.json`, regenerated by the Rust crawler.
- Source-health metadata shown in the app sidebar.
- Live public-source ingestion for Hacker News, Lobste.rs, and dev.to, merged with the curated watchlist for Pydantic, LakeSail, Turso, LanceDB, HelixDB, SurrealDB, pgGraph, Grust, and TypeSec.

## Next Work

- Add feed adapters for Medium and Substack.
- Add authenticated or policy-aware adapters for LinkedIn and X/Twitter.
- Add newsletter draft generation and Ghost API publishing.
- Extract shared UI/API/crawler modules back into Greathouse after the Verdun shape stabilizes.

## Verification

Current local checks:

- `npm run build`
- `npm run prod:build`
- `cargo check --manifest-path crawler/Cargo.toml`
- `cargo run --manifest-path crawler/Cargo.toml -- verify`
- `cargo run --manifest-path crawler/Cargo.toml -- collect --live --max-live-per-project 2`
- `cargo run --manifest-path crawler/Cargo.toml -- export-sql --input crawler/data/items.json --out /tmp/verdun-newsletter-load.sql`
- `npm run smoke:app -- http://127.0.0.1:5174`
